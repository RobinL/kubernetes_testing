apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: print_hello_world
spec:
  schedule: "*/1 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: print_hello_world
            image: robinlinacre/test_rpy
          restartPolicy: OnFailure
